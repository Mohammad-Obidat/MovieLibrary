<Page x:Class="MovieLibrary.Views.AddMoviePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Add Movie">
    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" Orientation="Vertical" Margin="0,0,12,0">
            <TextBlock Text="Title:"/>
            <TextBox x:Name="TxtTitle" />
            <TextBlock Text="Director:" Margin="0,8,0,0"/>
            <TextBox x:Name="TxtDirector" />
            <TextBlock Text="Year:" Margin="0,8,0,0"/>
            <TextBox x:Name="TxtYear" />
            <TextBlock Text="Genre:" Margin="0,8,0,0"/>
            <TextBox x:Name="TxtGenre" />
            <TextBlock Text="Duration (minute):" Margin="0,8,0,0"/>
            <TextBox x:Name="TxtDuration" />
            <TextBlock Text="Rating (0-10):" Margin="0,8,0,0"/>
            <TextBox x:Name="TxtRating" />
            <TextBlock Text="Cover image (URL):" Margin="0,8,0,0"/>
            <TextBox x:Name="TxtCoverUrl" />
            <Button Content="Add Movie" Margin="0,12,0,0" Click="BtnAdd_Click"/>
        </StackPanel>

        <Border Grid.Column="1" Background="#EEE" Padding="8">
            <TextBlock Text="Preview: Enter the image link and click Add to see the movie in the list." TextWrapping="Wrap"/>
        </Border>
    </Grid>
</Page>
